name: 'spark_snowflake_pipeline'
version: '1.0.0'
config-version: 2

profile: 'spark_snowflake_pipeline'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  spark_snowflake_pipeline:
    staging:
      +materialized: view
    marts:
      +materialized: table
